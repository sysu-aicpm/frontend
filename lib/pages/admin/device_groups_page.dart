import 'package:flutter/material.dart';

class DeviceGroupsPage extends StatelessWidget {
  const DeviceGroupsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("TODO: DeviceGroupsPage");
  }
} 